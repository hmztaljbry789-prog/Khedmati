const SAFE=new Set(["GET","HEAD","OPTIONS"]);export function originGuard(req,res,next){if(SAFE.has(req.method))return next();const a=(process.env.CLIENT_URL||"").split(",").map(v=>v.trim()).filter(Boolean);if(!a.length)return process.env.NODE_ENV==="production"?res.status(500).json({success:false,message:"CLIENT_URL is not configured"}):next();const o=req.get("origin");if(!o||!a.includes(o))return res.status(403).json({success:false,message:"Request origin is not allowed"});next();}
